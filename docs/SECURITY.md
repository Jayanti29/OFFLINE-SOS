# Security and Privacy

SOS content should remain local by default. A future encrypted message service must use a key supplied through secure local configuration and must never commit keys. Private records should not be synchronized without explicit user consent.
