# Offline Mode

Routing, SQLite access, safety calculations, emergency records, and route history are local operations. The UI status is `OFFLINE` and says it is using cached/local data. Synchronization is an optional enhancement and must never be required to calculate a route.
